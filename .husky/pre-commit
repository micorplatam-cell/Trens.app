echo "🔍 Ejecutando type-check antes de commit..."
npm run type-check

if [ $? -ne 0 ]; then
  echo "❌ ERROR: Type-check falló. Por favor corrige los errores de TypeScript antes de hacer commit."
  exit 1
fi

echo "✅ Type-check exitoso. Procediendo con commit..."
